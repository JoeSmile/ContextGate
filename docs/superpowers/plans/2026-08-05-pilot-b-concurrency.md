# 并发与一致性（链 A / Runner 必做 · 避免链 B 返工）

> 状态:**已吸收进 Task 40 AC**（2026-08-05）  
> 原则:安全判定**实时查**；列表可用短 TTL 快照；定义变更用乐观锁；run 级状态原子迁移。  
> 挂接:spec §10.4 · Task 40 各 Wave · master D1 修订

## 取舍

| 档 | 项 | 策略 | 落点 |
|----|-----|------|------|
| **必加（硬）** | 1 草稿丢更新 | PATCH `base_version`，不匹配 → **409** | 40.31 |
| **必加（硬）** | 2 编辑 vs 发布 | 发布=原子快照 IR + `version+1`；published **冻结**；改动 → 开新 draft | 40.31 / 40.57 |
| **必加** | 5 同 run 双执行 | `status` CAS 或 `FOR UPDATE`；`pending\|suspended→running` 仅一成功 | 40.35 / 40.44 |
| **必加** | 6 双 approve | `UPDATE … WHERE status=pending`；第二 → no-op/**409** | 40.44 |
| **必加** | 7 approve vs reject | 同状态机，只一终态；另一「已处理」 | 40.44 |
| **必加** | 8 批时角色已变 | **approve 时刻实时**校验业务角色（**不**用 org TTL 快照做安全判定） | 40.44 + S* |
| **必加** | 11 快照越权窗 | D1 快照**只用于列表/展示**；审批/二次鉴权/放行 **永远实时查** | 40.12 / 40.44 |
| **必加** | 12 重复挂起申请 | `UNIQUE(run_id, node_id)` on permission_request | 40.42 |
| **建议** | 9 移/删部门 | 移父=原子更新子树 `path`；有 members/published WF/在飞 run → 拒或软删 | 40.10 / 40.11 |
| **建议** | 13 迁移撞号 | Wave **固定 Alembic 号**（见全局约束）；并行 agent 禁止自编 00N | 40 索引 |
| **轻量** | 3 双发布 | 靠 version CAS → 第二 **409**（与 1/2 同机制） | 40.31 |
| **轻量** | 10 兼岗+主部门 | 「换主 + 授角色」**单事务** | 40.11 |
| **锦上添花 / 可后置** | 4 双击运行 | FE 防抖 + 同 user+workflow 1s 窗口 — **不挡 7A**；有空再做 | 40.36 / 40.52 标 Optional |

**优先级备注（2026-08-05）：** 主人拍板——**乐观锁（#1/#2/#3）> 双击限流（#4）**；#4 降为体验锦上添花，W3/W5 可不做。

## 已有、不重复造

- Run **IR 快照**（定义后改不影响在飞）— 40.33  
- 写节点 `(run_id, node_id)` 幂等 — 40.34（**不**替代 run 级 CAS）

## 链 B 预留（本文件不挡 7A，但契约已兼容）

机侧多 worker / 定时并发起 run：同样遵守 **4（限流窗口可调）+ 5（run CAS）+ 节点幂等**；不要到 2B 再改状态机。
