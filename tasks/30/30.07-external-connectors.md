# 30.07: 外部应用连接器(Dify/Coze)(后端)

> 阶段: 1a | 依赖: 30.03 | 预计: 1 commit

> 现状: 无 Dify/Coze 代码;harness 的 key 管理/failover 可复用。

**方案:**
1. `backend/core/capability/connectors/external_app.py`:
   - `DifyConnector`: `POST {base_url}/v1/workflows/run`(Bearer key),SSE 透传转统一格式
   - `CozeConnector`: 同模式
   - 超时/重试/断路器复用 `circuit_breaker.py`;凭证经 KeyManager 解密(30.03)
2. 审计带 `upstream=provider`;成本=上游计费(配 cost_per_1k 估算或上游返回)
3. config.env.example 示例注册(注释掉):
   `CAPABILITY_REGISTRY_JSON=[{"id":"dify-contract-review","kind":"external_app","provider":"dify","base_url":"https://api.dify.ai/v1","api_key_ref":"DIFY_API_KEY","permission":"chat:write"}]`

**修改文件:** `backend/core/capability/connectors/external_app.py`(新建), `config.env.example`

## AC(全绿才算完成)

- [x] 单测: mock 上游(LLM_PROVIDER 同思路)验证请求构造 + SSE 格式转换 + 审计 upstream 字段(见 30.27)
- [x] 断路器/超时路径生效(上游挂掉 → CAP_003,不 hang)
- [x] 无真实 Dify 实例也可过单测;有实例时手动 curl 验证(可选)
- [x] 已 commit
