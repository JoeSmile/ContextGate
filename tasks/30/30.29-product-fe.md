# 30.29: 产品 FE——能力市场 + 工作台 + 治理中心(阶段 2 蓝图)

> 阶段: 2 扩展 | 依赖: 阶段 1 全部 + 测试 FE 实测 journeys 剧本 | **不在本轮**

> **明确不在本轮 Cursor 范围**。等测试 FE 实测出别扭点清单、合并进本蓝图后启动。

**蓝图(按实测产出调整):**
1. **能力市场**: capabilities 面板升级为 Dify 风格卡片网格(筛选 + 搜索 + 详情抽屉 + "打开"跳工作台)
2. **工作台**: 气泡对话形态(Agent/能力选择器 + 打字机流式批处理 ≥10 token/帧 rAF 节流 +
   消息虚拟滚动 50 条以上 + 元数据按角色折叠 + 会话历史 react-query 缓存)
3. **治理中心**: audit/performance 面板升级(过滤器 + 分页 + CSV 导出 + 预算执行率进度条 +
   按租户/能力月度分摊表;数据源从 /performance/metrics 切换为审计成本——验收 P2-8)
4. **管理台升级**:
   - users.tsx: 用户管理(users 表 models.py:130 已有,无 role/tenant 列 + api_keys 派生;
     需新增 `PATCH /api/admin/api-keys/{id}/role`——验收 P2-2)
   - api-keys: + last_used_at(按 user 粒度聚合,audit_logs 无 key 列——验收 P2-3)+ 可选过期时间
   - capabilities 管理: 注册/停用/配额设置(写 DB,env 项只读标"env 管理")
   - approvals: 能力申请/审批(复用 approval_requests + 既有 /api/admin 审批端点)
5. **角色→落地页 + 侧边栏裁剪**: user → 工作台;admin → 治理中心;auditor → 治理中心(只读)。
   测试 FE 角色切换器保留为 admin 调试模式,不删除。

**修改文件(启动时):** `frontend/src/pages/market/**`, `workspace/**`, `governance/**`,
`admin/**`, `router.tsx`;后端 `PATCH /api/admin/api-keys/{id}/role`

## AC(启动时定义,本轮不验收)

- [ ] journeys 剧本 4 角色端到端验收通过
- [ ] 能力注册写 DB 重启仍在;审批跨租户过滤正确
- [ ] 测试 FE 角色切换器仍可切换(调试模式)
