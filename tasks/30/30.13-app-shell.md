# 30.13: AppShell + 路由 + 登录页(前端)

> 阶段: 1c | 依赖: 30.11 | 预计: 1 commit

> 现状: 无统一导航;散装 HTML 各是各的页面。

**方案:**
1. `components/layout/AppShell.tsx`: Topbar(logo · 环境徽章 dev/test/demo · 角色切换器占位 · 用户菜单)
   + 侧边栏(全功能入口: Chat/RAG/Admin/Audit/Agent/Eval/性能/能力)+ `<Outlet/>`
   - 环境徽章从 `GET /` 读取,dev=灰 / test=蓝 / demo=绿
2. `router.tsx`: `/login` + `/`(AppShell 包 panels);未登录(无 key)→ /login 回跳
3. `pages/login.tsx`: 居中卡片式(阿里云登录风格),4 角色 key 槽位 + 角色徽章
   (30.14 的切换器在此复用,登录即选角色)

**修改文件:** `frontend/src/components/layout/**`, `frontend/src/router.tsx`, `frontend/src/pages/login.tsx`

## AC(全绿才算完成)

- [x] 未登录访问 `/` → 跳 `/login`;填 key → 回跳原页
- [x] AppShell 侧边栏 8 个入口可见;环境徽章正确(dev=灰)
- [x] 登录页 4 角色槽位渲染,填一个 key 即可进
- [x] 已 commit

> Vite SPA 占用 `/`，环境 JSON 经 proxy `/cg-meta` → 后端根；缺 `environment` 字段时默认 `dev`。
