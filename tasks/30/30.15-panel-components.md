# 30.15: RequestPanel + SSEPanel 通用组件(前端)

> 阶段: 1c | 依赖: 30.09, 30.14 | 预计: 1 commit

> 现状: 无。8 个面板需要统一骨架,禁止每面板独写样式(Pitfall 14)。

**方案:**
1. `components/panels/RequestPanel.tsx`: 通用"请求表单 + 响应视图"骨架
   - props: `title, fields(表单字段定义), endpoint, method, onSend, renderResult`
   - 内置: 发送按钮、响应 pre、**403 高亮**(30.14)、耗时/状态码标注、角色徽章
2. `components/panels/SSEPanel.tsx`: 流式专用(打字机渲染 + 心跳指示 + abort + 三态徽章)
3. `components/ui/StatusBadge.tsx`: 成功/警告/错误/进行中四态徽章(不只靠颜色,附文字)

**修改文件:** `frontend/src/components/panels/RequestPanel.tsx`, `SSEPanel.tsx`,
`frontend/src/components/ui/StatusBadge.tsx`

## AC(全绿才算完成)

- [x] RequestPanel 渲染: 表单 + 发送 + 响应区;403 红字;耗时/状态码标注
- [x] SSEPanel 渲染: 打字机 + 心跳指示 + abort 按钮
- [x] 用假数据截图自查空态/错误态/加载态三态齐全
- [x] 已 commit

> Chat 面板挂载双组件作预览；真流式对接 30.16。
