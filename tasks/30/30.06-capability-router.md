# 30.06: capability router + LangFuse + app.py 挂载(后端)

> 阶段: 1a | 依赖: 30.04, 30.05 | 预计: 1 commit

> 现状: 无 capability 端点;_inject_langfuse_parent 在 pipeline/router.py:181(pipeline 层)。

**方案:**
1. `backend/routers/capability.py`:
   - `POST /api/capabilities/{id}/invoke`(SSE,复用 /chat/streaming 事件格式与断开中止逻辑)
   - `GET /api/capabilities`(市场列表,按角色过滤可见性)
   - **此文件负责**: 挂 LangFuse 根 trace(`_inject_langfuse_parent` 同款逻辑)+ SSE 帧组装 +
     客户端断开中止——routers 层允许 import pipeline,core 只做纯分发(接缝 1)
2. `backend/app.py`: `_lazy_include` 注册 capability router(可选路由,与 RAG/Agent 同模式)

**修改文件:** `backend/routers/capability.py`(新建), `backend/app.py`

## AC(全绿才算完成)

- [x] 启动后 `GET /api/capabilities` 返回列表(按角色过滤)
- [x] invoke 端点: 长路径 SSE 流式 + 短路径 JSON 双格式;`: ping` 心跳正常;断连中止
- [x] LangFuse 长路径 trace 呈完整 span 树(短路径采样 0.1 缺失不是 bug)
- [x] `make check`(ruff + mypy)通过
- [x] 已 commit

> 注: LangFuse 树依赖运行时采样/密钥；代码侧已挂 `@observe` + `_inject_langfuse_parent` + flush/discard。
