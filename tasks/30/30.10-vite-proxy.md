# 30.10: vite proxy + 目录骨架(前端)

> 阶段: 1b | 依赖: 30.08 | 预计: 1 commit

> 现状: 无配置;后端 /health 无 /api 前缀(验收 P2-5)。

**方案:**
1. `vite.config.ts` proxy:
   ```ts
   server: { port: 5173, proxy: {
     '/api': 'http://localhost:8000',                    // admin/ab/audit/feedback/eval/personalization/rag/capabilities
     '/chat': 'http://localhost:8000', '/streaming': 'http://localhost:8000',
     '/enhanced-chat': 'http://localhost:8000', '/agent': 'http://localhost:8000',
     '/memory': 'http://localhost:8000', '/performance': 'http://localhost:8000',
     '/health': 'http://localhost:8000',                 // 登录验证用,无 /api 前缀
     '/playground': 'http://localhost:8000',
   }}
   ```
2. 目录骨架: `src/api/` `src/hooks/` `src/stores/` `src/pages/` `src/components/`
   `src/components/ui/` `src/types/`

**修改文件:** `frontend/vite.config.ts`, 目录骨架

## AC(全绿才算完成)

- [ ] `npm run dev` 后 `curl localhost:5173/health` 转发到 8000 返回 200
- [ ] `curl localhost:5173/api/rag/status` 同样转发(有 key 或无 key 都看得到转发本身)
- [ ] 目录骨架存在,`src/types/` 有后端响应类型占位
- [ ] 已 commit
