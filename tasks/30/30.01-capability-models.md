# 30.01: Capability 数据模型 + 错误码(后端)

> 阶段: 1a 后端能力层 | 依赖: 无 | 预计: 1 commit
> 总纲: `tasks/30-capability-hub-frontend.md`

> 现状: `backend/core/model_registry.py` 只有 ModelSpec;无统一能力模型。

**方案:**
1. 新建 `backend/core/capability/__init__.py` + `models.py`:
   - `CapabilitySpec` 数据类: `id, name, kind(model|datasource|tool|workflow|external_app|agent),
     provider(contextgate|dify|coze|ai-platform|self-hosted), spec(dict), status, cost_model, permission`
2. 新建 `errors.py`: `CAP_001 capability_not_found` / `CAP_002 capability_disabled` /
   `CAP_003 upstream_error` / `CAP_004 governance_required` / `CAP_005 quota_exceeded`
   (继承 ContextGateException,错误码格式与现有 AUTH_001/LLM_002 一致)

**修改文件:** `backend/core/capability/__init__.py`(新建), `models.py`(新建), `errors.py`(新建)

## AC(全绿才算完成)

- [x] `uv run ruff check backend/core/capability/` 通过
- [x] `CapabilitySpec` 可实例化,6 种 kind 均可构造
- [x] 5 个错误码继承 ContextGateException,格式与现有错误码一致
- [x] 已 commit(Conventional Commits + Signed-off-by: Joe)
