# 30.14: 角色切换器(测试 FE 灵魂)(前端)

> 阶段: 1c | 依赖: 30.11, 30.13 | 预计: 1 commit | **测试 FE 核心价值**

> 现状: 30.13 只放了占位。这是测试 FE 与产品 FE 的本质区别所在
> (测试 FE = 同一界面 + 角色切换 + 403 高亮,不是角色裁剪, Pitfall 16)。

**方案:**
1. `components/role/RoleSwitcher.tsx`:
   - 下拉列出 4 角色槽位(已填 key 的显示徽章,未填的显示"未配置")
   - 一键切换 → `switchRole(role)` → 所有请求自动带新 key,面板自动刷新
   - 角色徽章颜色: user 灰 / tadmin 蓝 / auditor 紫 / super 橙
2. **403 高亮**: http.ts(30.11)返回结构化 `{forbidden: true, needed}` →
   RequestPanel(30.15)红字显示"该角色无权限(需 xxx)" + 提示切换角色

**修改文件:** `frontend/src/components/role/RoleSwitcher.tsx`, `frontend/src/api/http.ts`

## AC(全绿才算完成)

- [x] 4 槽位下拉渲染;切换后激活徽章变化,请求自动带新 key
- [x] 切 user → 打开 Admin 面板 → 403 红字"该角色无权限";切 super → 可用
- [x] 单测(见 30.26): 切换后请求带新 key、403 高亮状态
- [x] 已 commit

> auditor 徽章用青（chart-4）而非紫，对齐 UX「无紫色残留」。UI RTL 单测留 30.26。
