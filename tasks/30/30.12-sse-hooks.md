# 30.12: useSSEStream + useChatStream(前端核心资产)

> 阶段: 1c 前端核心 | 依赖: 30.11 | 预计: 1 commit

> 现状: 无。SSE 事件格式(后端 /chat/streaming): `{"token": ...}` / `: ping` 心跳 /
> abort / retraction / error / `[DONE]`;短路径返回 `application/json`(Pitfall 2 双格式)。

**方案:**
1. `src/hooks/useSSEStream.ts`: **手写 SSE 解析器**(单文件 ≤100 行,项目核心资产)
   - fetch + ReadableStream + TextDecoder;按 `\n\n` 切帧;忽略 `: ping` 注释行(Pitfall 3);
     `data: ` 剥离;`[DONE]` 收尾
   - 回调: `onToken(text) / onAbort(reason) / onRetraction(reason) / onError(code,msg) / onDone(meta)`
   - `AbortController` 中断;先读 Content-Type——`text/event-stream` 流式 / `application/json` 直取 response
2. `src/hooks/useChatStream.ts`: 发送 → 消息数组更新流程(供 chat 面板用)

**修改文件:** `frontend/src/hooks/useSSEStream.ts`, `frontend/src/hooks/useChatStream.ts`

## AC(全绿才算完成)

- [x] 单测(见 30.26,先留 TODO): mock ReadableStream 喂 token/心跳/abort/error/[DONE]/JSON 双格式
- [x] `: ping` 注释行被忽略(不触发 onToken、不报错)
- [x] abort 后流中断且回调正确
- [x] 已 commit

> 解析纯函数在 `sseParse.ts`；`useSSEStream.ts` ≤100 行。帧级测已覆盖 ping/abort；完整 ReadableStream mock 留 30.26。
