# 30.04: invoke 通道核心分发 + 成本幂等(后端)

> 阶段: 1a | 依赖: 30.02 | 预计: 1 commit

> 现状: /chat/streaming(pipeline/router.py)已有 SSE 长路径真流式,事件格式
> `{"token": ...}` / `: ping` 心跳 / abort / retraction / error / `[DONE]`;短路径返回普通 JSON。
> harness.stream 已抽象 provider(harness/llm.py:117)。

**方案:**
1. `backend/core/capability/invoke.py`: `invoke(cap_id, payload, tenant, user) -> AsyncIterator[SSE 帧]`
   - 查 registry → 校验 permission(复用 `require_permission`)→ 按 kind 分发:
     - kind=model: 走 harness.stream(原样)
     - kind=external_app: 转发上游(30.07 连接器)
     - kind=agent: 调 Agent 层(30.24)
   - **分层约束(接缝 1)**: core/capability 禁止 import pipeline;LangFuse 根注入 + SSE 组帧在
     routers 层(30.06)
   - **成本幂等(接缝 4)**: record_consumption 只在 harness 内部记(harness/llm.py:94/198);
     invoke 层对 kind=model 不重复记,只对 external_app/agent 记账,
     审计带 `cost_source: "harness"|"invoke"`

**修改文件:** `backend/core/capability/invoke.py`(新建)

## AC(全绿才算完成)

- [x] kind=model 分发到 harness.stream;不产生 invoke 层 cost 记录(单测断言,见 30.27)
- [x] permission 校验失败返回 403 语义错误
- [x] 未知 kind / 不存在 cap → CAP_001;disabled → CAP_002
- [x] 单测: mock registry + harness,断言分发与幂等(见 30.27,先留 TODO)
- [x] 已 commit

> TODO(30.27): pytest 覆盖 model 分发 / AUTH_002 / CAP_001 / CAP_002 / cost_source=harness。
