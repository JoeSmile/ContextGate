# 30.03: config.env 注册项 + 凭证接入(后端)

> 阶段: 1a | 依赖: 30.02 | 预计: 1 commit

> 现状: config.env 无能力注册配置;外部凭证无统一入口。

**方案:**
1. `config.env` + `config.env.example` 新增:
   ```
   CAPABILITY_REGISTRY_JSON=[]   # [{"id":"dify-contract","kind":"external_app","provider":"dify","base_url":"...","api_key_ref":"DIFY_API_KEY","permission":"chat:write"}, ...]
   CAP_QUOTA_DAILY_CALLS=1000
   CAP_QUOTA_DAILY_COST_USD=10.0
   ```
2. 外部凭证走 KeyManager 加密库(复用 Task 18 的 LLM_KEY_MASTER_KEY 机制),env 明文兜底
   (registry.py 加 `resolve_credential(api_key_ref)` 辅助,调 KeyManager)。

**修改文件:** `config.env`, `config.env.example`, `backend/core/capability/registry.py`

## AC(全绿才算完成)

- [x] 无 CAPABILITY_REGISTRY_JSON 时 registry 空启动不报错
- [x] 有值时加载成功;凭证可经 KeyManager 解析(无 master key 时 env 明文兜底)
- [x] 配额两个 env 键生效(30.05 使用)
- [x] 已 commit
