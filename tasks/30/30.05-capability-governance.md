# 30.05: 治理强制——护栏/限流/配额(后端)

> 阶段: 1a | 依赖: 30.04 | 预计: 1 commit | **安全关键(接缝 3)**

> 现状: guardrails/rate_limiter 是 LangGraph 管线节点;非 model 类型绕过管线 = 治理旁路。
> 注意: Task 29 限流是固定分钟桶 INCR+EXPIRE(rag/cache.py:349-372,非 zset 滑动窗口,验收 P2-4);
> `core/rate_limiter.py:41` 是内存 TokenBucket(非 redis),勿混用。

**方案:**
1. 从 `backend/core/guardrails/` 抽公共函数(不依赖图节点):
   - 入向: input_guard.check_input 复用;出向: output_guard.check_output 复用
2. redis **分钟桶计数限流**(INCR+EXPIRE,复用 Task 29 `rl:rag:*` 模式,key 加 `cap:` 前缀)
3. 配额: per-tenant 日调用次数 / 日成本上限(30.03 env),redis day bucket,超限 `CAP_005`
4. 注册时校验: 非 model 能力不声明治理 → 拒绝注册 `CAP_004`

**修改文件:** `backend/core/capability/governance.py`(新建,公共函数)

## AC(全绿才算完成)

- [x] 单测: 超配额 → CAP_005;未声明治理注册 → CAP_004(见 30.27,先留 TODO)
- [x] 限流用 redis 分钟桶(INCR+EXPIRE),不用内存 TokenBucket
- [x] guardrails 抽公共函数后,原图节点行为不变(现有测试守护)
- [x] 已 commit

> TODO(30.27): pytest 覆盖 CAP_004 注册拒绝 / CAP_005 配额 / 分钟桶限流。
