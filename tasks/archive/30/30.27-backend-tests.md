# 30.27: 后端单测(后端)

> 阶段: 1f | 依赖: 30.01-30.07 | 预计: 1 commit

**方案:** `tests/test_capability.py`(沿用现有桩模式,不真连 PG/网络):
- registry: 注册/查寻/DB 优先 env/权限拒绝
- invoke 分发: kind=model → harness;未知 kind → CAP_001;disabled → CAP_002
- 成本幂等: kind=model 无 invoke 层 cost 记录(接缝 4)
- 治理: 超配额 → CAP_005;未声明治理注册 → CAP_004
- 外部应用 mock 转发: 请求构造 + SSE 格式转换 + 审计 upstream
- Agent: 递归上限 3、自引用拒绝

**修改文件:** `tests/test_capability.py`(新建)

## AC(全绿才算完成)

- [ ] `uv run pytest tests/test_capability.py -q` 全绿
- [ ] `uv run pytest` 全量通过(原有用例不破)
- [ ] `uv run ruff check backend/ scripts/ && uv run mypy` 通过
- [ ] 已 commit
