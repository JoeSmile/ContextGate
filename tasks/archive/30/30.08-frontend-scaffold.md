# 30.08: 前端脚手架(Vite + React 19 + TS + shadcn)

> 阶段: 1b 前端基建 | 依赖: 无 | 预计: 1 commit

> 现状: 项目无 package.json(Node v25.6.1 已就绪);旧 React 前端已删(3654e68),不参考。

**方案:**
```bash
mkdir frontend && cd frontend
npm create vite@latest . -- --template react-ts
npm i zustand @tanstack/react-query react-router-dom @radix-ui/react-dialog @radix-ui/react-select @radix-ui/react-tabs @radix-ui/react-switch @radix-ui/react-label @radix-ui/react-slot @radix-ui/react-dropdown-menu @radix-ui/react-toast class-variance-authority clsx tailwind-merge lucide-react
npm i -D @tailwindcss/vite tailwindcss
npx shadcn@latest init
```

**修改文件:** `frontend/**`(全部新建)

## AC(全绿才算完成)

- [x] `cd frontend && npm run dev` 出 Vite 默认页(5173)
- [x] `npm run build` 通过(tsc --noEmit + vite build)
- [x] shadcn init 完成,`components/ui/` 有骨架组件
- [x] 已 commit

> 注: Radix Nova 预设下 toast 改用 `sonner`(shadcn 提示);骨架含 button/card/table/badge/input/dialog/tabs/sonner 等。
