# 30.24: Agent 门面 + seed(后端)

> 阶段: 1e Agent | 依赖: 30.02, 30.04 | 预计: 1-2 commit

> 现状: **仓库存在两套 Agent(验收 P1-2)**:
> - `backend/agent/`(V2 Runtime: agent_core/memory_hub/planner/reflector/tool_caller)——经
>   `backend/routers/agent.py` 挂载,**是 /agent/* 的真实实现**;`backend/services/agent_service.py:9`
>   import 它(`from backend.agent import AgentCore, get_agent_core`)。
> - `backend/modules/agent/`(core/models/protocol/routers/services 全套)——routers/agent_router.py
>   **未被 app.py 挂载,孤儿副本**。本轮不动不包装,文档注明"未挂载,勿引用"。

**方案:**
1. `backend/core/capability/agents.py`:
   - `AgentSpec`: `{id, name, role, system_prompt_ref, capabilities: [cap_id...], memory: bool}`
   - `AgentRuntime.invoke(agent_id, payload)`: **门面模式**包装
     `AgentService.process_message()`(不重写核心逻辑,已有会话/记忆/状态管理)
     - **capabilities 注入(验收 P2-1)**: process_message(user_id, message, conversation_id)
       无现成注入点 → 加可选 kwargs `capabilities: list[str] = None`;AgentCore.process 侧同样加可选参数透传
     - 审计 `action="agent.invoke"` + 成本(invoke 层记账,接缝 4)
     - **流式化(架构评审 P1)**: process_message 非流式(返回完整字符串)→ 门面内分片转发
       (harness mock 切片思路);子能力流按到达顺序聚合,保持"调用链可见";优先测通
       "vendor-risk → contract-query → rag-ask" 完整链
     - Agent 调 Agent: 递归 invoke,深度上限 3,防环(能力集引用自身启动校验拒绝)
2. `scripts/seed_capabilities.py`: 演示 Agent 写 capabilities 表(kind=agent,验收 P2-7,不 env 硬编码):
   - `contract-query-agent`(能力 = rag-ask + contextgate-chat)
   - `vendor-risk-agent`(能力 = contract-query-agent + contextgate-chat)——**Agent 调 Agent 演示**
3. 旧 /agent 路由标 deprecated(保留,文档注明)

**修改文件:** `backend/core/capability/agents.py`(新建), `scripts/seed_capabilities.py`(新建),
`backend/services/agent_service.py`(加 kwargs)

## AC(全绿才算完成)

- [ ] seed 后 GET /api/agents 列出 2 个演示 Agent
- [ ] 调 vendor-risk-agent → 流式返回 + 审计三条记录(vendor-risk → contract-query → rag-ask)
- [ ] 递归上限 3 生效;自引用能力拒绝注册
- [ ] modules/agent 未被 import/包装(文档注明即可)
- [ ] 已 commit
