# 30.26: 前端单测(前端)

> 阶段: 1f 收尾 | 依赖: 30.12-30.15 | 预计: 1 commit

**方案:** vitest + testing-library:
- useSSEStream: mock ReadableStream 喂 token/心跳/abort/error/[DONE]/JSON 双格式
- authStore: persist 读写/清除/4 槽位切换互不覆盖
- RoleSwitcher: 切换后请求带新 key、403 高亮状态
- 面板 smoke: 渲染 + 空态

**修改文件:** `frontend/src/**/*.test.ts(x)`(新建)

## AC(全绿才算完成)

- [ ] `cd frontend && npm run test` 全绿
- [ ] SSE 各帧型用例齐全(含 `: ping` 忽略、双格式)
- [ ] 已 commit
